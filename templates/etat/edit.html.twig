{% extends 'base.html.twig' %}

{% block title %}Edit Etat{% endblock %}

{% block body %}
    <div class="container mx-auto px-6 py-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-8">Edit Etat</h1>

        <div class="bg-white shadow-lg rounded-lg p-8 border border-gray-200">
            {{ include('etat/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="mt-8 flex flex-wrap items-center space-x-4">
            <a href="{{ path('app_etat_index') }}" class="inline-block bg-gray-600 text-white py-2 px-4 rounded shadow hover:bg-gray-700">
                Back to list
            </a>

            {{ include('etat/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
