{{ form_start(form, { attr: { class: 'space-y-6' } }) }}
<div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
    {{ form_widget(form, {
        attr: {
            class: 'block w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500 text-gray-700 bg-gray-50'
        }
    }) }}
</div>
<button class="bg-blue-600 text-white py-2 px-4 rounded shadow hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
    {{ button_label|default('Save') }}
</button>
{{ form_end(form) }}
