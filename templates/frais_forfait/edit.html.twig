{% extends 'base.html.twig' %}

{% block title %}Edit FraisForfait{% endblock %}

{% block body %}
    <div class="container mx-auto mt-6 p-6 bg-white rounded-lg shadow">
        <h1 class="text-2xl font-semibold mb-4">Edit FraisForfait</h1>

        <div class="mb-6">
            {{ include('frais_forfait/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="flex justify-between items-center mt-4">
            <a href="{{ path('app_frais_forfait_index') }}" class="text-blue-600 hover:underline">Back to list</a>
            {{ include('frais_forfait/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}