{% extends 'base.html.twig' %}

{% block title %}Edit FicheFrais{% endblock %}

{% block body %}
    <div class="container mx-auto px-4 py-6">
        <h1 class="text-3xl font-bold text-gray-800 mb-6">Edit FicheFrais</h1>

        <div class="bg-white shadow-md rounded-lg p-6 border border-gray-200">
            {{ include('fiche_frais/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="mt-6 space-x-4">
            <a href="{{ path('app_fiche_frais_index') }}" class="inline-block bg-gray-600 text-white py-2 px-4 rounded shadow hover:bg-gray-700">
                Back to list
            </a>

            {{ include('fiche_frais/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
